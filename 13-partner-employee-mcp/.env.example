# Copy to .env (gitignored) and fill in. Never commit real keys.
#   cp .env.example .env

# Anthropic — employee upstream for public model "assistant"
# Upstream model: claude-sonnet-4-6
ANTHROPIC_API_KEY=

# xAI — partner / consultant / missing-header upstream
# Upstream model: grok-4.6
XAI_API_KEY=
