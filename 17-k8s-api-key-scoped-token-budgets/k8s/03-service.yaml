apiVersion: v1
kind: Service
metadata:
  name: agentgateway
  namespace: agw-token-budgets
spec:
  selector:
    app: agentgateway
  ports:
  - name: llm
    port: 4000
    targetPort: llm
  - name: admin
    port: 15000
    targetPort: admin
