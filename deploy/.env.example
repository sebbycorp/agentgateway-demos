# Provider keys — set these in the PaaS dashboard, never commit real values.
# First boot writes /config/config.yaml with $OPENAI_API_KEY on the OpenAI
# wildcard model and $GITHUB_PERSONAL_ACCESS_TOKEN on GitHub remote MCP.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Also used if you add stdio @modelcontextprotocol/server-github later.
GITHUB_PERSONAL_ACCESS_TOKEN=

# UI basic auth (required). The entrypoint writes /config/.htpasswd from
# these on every start. Do not commit real passwords.
# Browser /ui/ prompts for this pair. LLM routes on :4000 do not.
UI_PASSWORD=
# Optional; defaults to admin.
UI_USER=admin
