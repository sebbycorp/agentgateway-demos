# Provider keys — set these in the PaaS dashboard, never commit real values.
# The generated /config/config.yaml expands $OPENAI_API_KEY (and friends)
# when you add a model in the UI or edit the file.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
