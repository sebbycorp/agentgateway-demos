# Thin wrapper so Railway (and Fly, if you prefer a Dockerfile) pull the
# official OSS image instead of rebuilding agentgateway.
# Pin: https://agentgateway.dev/docs/standalone/latest/setup/install/docker/
FROM cr.agentgateway.dev/agentgateway:v1.5.0

EXPOSE 4000
