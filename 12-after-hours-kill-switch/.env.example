# Copy to .env (gitignored) and fill in. Never commit real keys.
#   cp .env.example .env

# Anthropic — daytime upstream for public model "claude"
# Upstream model: claude-sonnet-4-6
ANTHROPIC_API_KEY=

# xAI — after-hours (and x-force-after-hours) upstream
# Upstream model: grok-4.6
XAI_API_KEY=
