# Solo Enterprise license key (required) — https://www.solo.io/company/contact
AGENTGATEWAY_LICENSE_KEY=
# OpenAI API key (required — /openai route, default model gpt-5.5)
OPENAI_API_KEY=
# Anthropic API key (required — /anthropic route, default model claude-opus-4-8)
ANTHROPIC_API_KEY=
