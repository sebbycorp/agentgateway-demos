# Copy to .env (gitignored) or export in your shell.
# Required for the live OpenAI path. setup.sh falls back to mock-openai.py
# when this is unset so the Block/Audit demo still runs.

OPENAI_API_KEY=
