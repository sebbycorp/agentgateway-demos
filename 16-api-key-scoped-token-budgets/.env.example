# Copy to .env (gitignored) or export in your shell.
# REQUIRED — the demo calls live OpenAI with this key. setup.sh exits if it is
# unset; use `USE_MOCK=1 ./setup.sh` only when you want an offline run against
# mock-openai.py.

OPENAI_API_KEY=
